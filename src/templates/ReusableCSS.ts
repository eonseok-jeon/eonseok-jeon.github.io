import styled from 'styled-components';

const Container = styled.section`
  width: 100%;
  padding: 0 5rem;
`;

const SubTitle = styled.h3`
  font-size: 3.2rem;
  font-weight: 500;
`;

export {
  Container,
  SubTitle
};